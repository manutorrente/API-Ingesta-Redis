# Redis Ingestion API Configuration Example
# Copy this file to .env and update the values according to your setup

# API Authentication
# Username for basic authentication
API_USERNAME=your_api_username

# Bcrypt hashed password for basic authentication
# Generate using: python3 -c "import bcrypt; password = input('Enter password: '); print('Hashed:', bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt()).decode('utf-8'))"
API_PASSWORD=your_bcrypt_hashed_password_here

# Server Configuration
# Host to bind the API server (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
API_HOST=0.0.0.0

# Port for the API server
API_PORT=8000

# Redis Ingestion Script Configuration
# Full path to the directory containing your Redis ingestion scripts
SCRIPT_PATH=/path/to/your/redis/scripts

# Name of the main script file to execute
SCRIPT_NAME=your_script_name.sh


